<?php

declare(strict_types=1);

// Placeholder for Laravel's public entry point.
// Run `php artisan serve` in a full Laravel install to serve the app.

echo "Real Estate Token Booking App backend is configured. Run within Laravel to serve.";
